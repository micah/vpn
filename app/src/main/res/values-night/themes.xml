<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TorWithArti" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/figma_bg_inverted</item>
        <item name="colorPrimaryVariant">@color/figma_gray_inverted</item>
        <item name="colorOnPrimary">@color/figma_text_inverted</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/figma_bg_inverted</item>
        <item name="colorSecondaryVariant">@color/figma_gray_inverted</item>
        <item name="colorOnSecondary">@color/figma_text_inverted</item>
        <!-- Status bar color. -->
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/colorIconPrimary</item>
        <!-- Customize your theme here. -->
    </style>
</resources>